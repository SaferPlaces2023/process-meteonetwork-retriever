import setuptools

VERSION = "1.0.2"
PACKAGE_REPO = "template-cli"
PACKAGE_NAME = PACKAGE_REPO.replace("-", "_")
AUTHOR = "Valerio Luzzi"
EMAIL = "valerio.luzzi@gecosistema.com"
GITHUB = f"https://github.com/saferplaces/{PACKAGE_REPO}.git"
DESCRIPTION = "Generic template for a CLI application using Click"

setuptools.setup(name=PACKAGE_REPO,
                 version=VERSION,
                 description=DESCRIPTION,
                 long_description=DESCRIPTION,
                 classifiers=[
                     "Programming Language :: Python :: 3",
                     "License :: OSI Approved :: MIT License",
                     "Operating System :: OS Independent",
                 ],
                 keywords='',
                 author=AUTHOR,
                 author_email=EMAIL,
                 url=GITHUB,
                 packages=setuptools.find_packages("src"),
                 package_dir={'': 'src'},
                 install_requires=[
                     'setuptools',
                     'wheel',
                     'requests',
                     'click',
                     'boto3',
                 ],
                 entry_points="""
        [console_scripts]
        template-cli=template_cli.main:main_click
        template_cli=template_cli.main:main_click
        """,
)
